{
  "name": "vibes-daemon",
  "version": "1.0.0",
  "description": "PC daemon that connects to a WebSocket relay server and executes claude -p commands from a smartphone",
  "main": "daemon.js",
  "scripts": {
    "start": "node daemon.js",
    "dev": "node --watch daemon.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
